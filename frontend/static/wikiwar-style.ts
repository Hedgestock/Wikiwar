import {Theme} from "@material-ui/core";

export const styles = (theme: Theme) => ({
    wwar_page: {
        margin: "8px",
    },
    wwar_menuItem: {
        textDecoration:"none",
        color:"inherit",
    },
    wwar_iframe: {
        width: "100%",
        height: "700px",
    },
});
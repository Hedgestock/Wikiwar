import {Theme} from "@material-ui/core";

export const styles = (theme: Theme) => ({
    wwar_page: {
        margin: "8px",
    },
    wwar_menuItem: {
        textDecoration:"none",
        color:"inherit",
    },
});